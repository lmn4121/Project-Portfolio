# Portfolio (Next.js on Vercel) — public only
NEXT_PUBLIC_TWIN_API_URL=http://localhost:8000

# Do NOT put OPENAI_API_KEY or PUSHOVER_* in this frontend env.
# Those belong on the twin-api host only.
